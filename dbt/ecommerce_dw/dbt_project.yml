name: "ecommerce_dw"
version: "1.0.0"
config-version: 2

profile: "ecommerce_dw"

model-paths:
  - "models"

test-paths:
  - "tests"

seed-paths:
  - "seeds"

snapshot-paths:
  - "snapshots"

macro-paths:
  - "macros"

target-path: "target"

clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  ecommerce_dw:
    staging:
      +materialized: view
      +schema: staging



    warehouse:
      +materialized: table
      +schema: warehouse

    marts:
      +materialized: table
      +schema: marts